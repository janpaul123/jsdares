#!/usr/bin/env node

var path = require("path");
var Reflect = require("./reflect").Reflect;

var source = read(process.argv[2]);
var target = Reflect.parse(source, true);

console.log(target);

function read (fpath) {
    return require("fs").readFileSync(path.normalize(fpath), "utf8");
}
